#include <iostream>
using namespace std;

int main(void)
{
	ios_base::sync_with_stdio(0);
	cin.tie(0);
	int N;
	cin >> N;

	cout << (N ? "Leading the Way to the Future" : "YONSEI") << "\n";

	return 0;
}